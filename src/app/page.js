import DockNavigationBar from "@/components/DockNavigationBar";

export default function HomePage() {
  return (
    <main className="bg-neutral-900 text-white min-h-screen flex flex-col items-center justify-center">
      <DockNavigationBar />
    </main>
  );
}
